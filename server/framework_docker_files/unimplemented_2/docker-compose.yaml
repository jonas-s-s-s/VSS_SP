name: unimplemented_2

services:
  hello-world:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    env_file:
      - ../config.env
    container_name: unimplemented_2

